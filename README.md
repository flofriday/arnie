# Arnie

A tool to benchmark original vadl and openVADL.
While original vadl isn't open source this tool is.

## Usage

```bash
uv run arnie.py config  # You only have to do that once
uv run arnie.py bench 
```
